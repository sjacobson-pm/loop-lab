/* v8 ignore start -- justification: this is simply a configuration file; no testing needed */

/**
 * Application-wide constants, including application name and abbreviation.
 * @type {Object}
 *
 * Properties:
 * - `APP_NAME` (string): The full name of the application.
 * - `APP_ABBR` (string): The abbreviation of the application name.
 */
export const constants = {
  APP_NAME: 'FakeAPI',
  APP_ABBR: 'WPT',
};
