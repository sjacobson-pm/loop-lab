---
id: US-002
title: CeilingTo rounds currency values to the nearest cent multiple
target_repo: loop-lab
target_profile: dotnet-xunit
spec_path: .loop/fixtures/spec/rounding.md
status: ready
---

# US-002 — CeilingTo rounds currency values to the nearest cent multiple

## Intent

Billing needs to round charge amounts up to a cent multiple before invoicing.
This story extends the rounding work to the currency case.

## Criteria

Cited by anchor. Do not restate.

- `.loop/fixtures/spec/rounding.md#ceiling-to-basic`
- `.loop/fixtures/spec/rounding.md#ceiling-to-scale`
- `.loop/fixtures/spec/rounding.md#ceiling-to-currency`

## Acceptance criteria

- Currency rounding uses the existing `CeilingTo(decimal, decimal)` surface;
  no new public method is introduced.
- Unit tests live in `tests/unit/RoundingTests.cs`.
- Every criterion above has at least one test that fails against the base
  commit and passes after implementation.

## Out of scope

- Currency formatting, symbols, or culture handling.
- Any change to files outside `src/` and `tests/unit/`.

## Notes

`PROJECT.md` is advisory context only and may not be cited as a criterion.
